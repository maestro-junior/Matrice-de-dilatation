% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/appliquer_matrice.R
\name{appliquer_matrice_dilatation}
\alias{appliquer_matrice_dilatation}
\title{Apply a Dilation Matrix}
\usage{
appliquer_matrice_dilatation(donnees_sf, matrice_dilatation)
}
\arguments{
\item{donnees_sf}{An sf object with spatial geometry data.}

\item{matrice_dilatation}{The dilation matrix.}
}
\value{
An sf object with dilated geometry.
}
\description{
This function applies a dilation matrix to the geometry in an sf object.
}
\examples{
library(sf)

# Create a sample sf object
sample_sf <- st_as_sf(
  data.frame(id = 1:2, x = c(0, 1), y = c(0, 1)),
  coords = c("x", "y"),
  crs = 4326
)

# Define a simple dilation matrix
dilation_matrix <- matrix(c(1.5, 0, 0, 1.5), nrow = 2)

# Apply the dilation matrix
dilated_map <- appliquer_matrice_dilatation(sample_sf, dilation_matrix)

# Print dilated map
print(dilated_map)
}
