% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/echelle_carte.R
\name{mise_echelle_carte}
\alias{mise_echelle_carte}
\title{Scale the Map}
\usage{
mise_echelle_carte(carte_donnees, facteur_dilatation)
}
\arguments{
\item{carte_donnees}{The spatial data in the form of an sf object.}

\item{facteur_dilatation}{Scaling factor. If >1, the map is enlarged;
if between 0 and 1, the map is reduced;
if =1, it remains unchanged.}
}
\value{
A resized sf object.
}
\description{
This function resizes an urban map generated from a graphical package
(e.g., ggplot2, sf) using a dilation matrix.
}
\examples{
library(sf)

# Create a sample sf object
sample_sf <- st_as_sf(
  data.frame(id = 1:2, x = c(0, 1), y = c(0, 1)),
  coords = c("x", "y"),
  crs = 4326
)

# Define a dilation factor (e.g., 1.5 for enlargement)
facteur_dilatation <- 1.5

# Scale the map
scaled_map <- mise_echelle_carte(sample_sf, facteur_dilatation)

# Print scaled map
print(scaled_map)
}
